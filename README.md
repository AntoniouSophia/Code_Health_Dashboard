ΟΔΗΓΙΕΣ ΕΓΚΑΤΑΣΤΑΣΗΣ

1. Απαραίτητο λογισμικό 
Πριν από την εκτέλεση της εφαρμογής πρέπει να εγκατασταθούν: 
- Git για τη λήψη των αποθετηρίων από το GitHub  
- Node.js 22.12 ή νεότερο, μαζί με το npm 
- Python 3 για την εκτέλεση των αρχείων ανάλυσης 
- XAMPP για την τοπική λειτουργία του MariaDB/MySQL server 
- HeidiSQL για τη δημιουργία, εισαγωγή και διαχείριση της βάσης δεδομένων (προτείνεται) 
- Java Development Kit συμβατό με την έκδοση του SonarQube 
- SonarQube Community Build το οποίο εκτελείται τοπικά 
- SonarScanner CLI για την αποστολή του κώδικα στο SonarQube 
- Maven ή Gradle όταν αναλύονται έργα Java που χρησιμοποιούν αντίστοιχο σύστημα build 
- Πρόσβαση στην υπηρεσία AllinCode και έγκυρα στοιχεία σύνδεσης 
-Σύνδεση στο διαδίκτυο κατά την εγκατάσταση και την εκτέλεση της εφαρμογής 
Το Git, η Python, το Node.js, το npm και η Java πρέπει να είναι διαθέσιμα από τη γραμμή εντολών. Το Maven ή το Gradle πρέπει επίσης να είναι διαθέσιμο, όταν απαιτείται από το Java project που πρόκειται να αναλυθεί.

2. Αποσυμπίεση του project 
Ο χρήστης κατεβάζει το αρχείο ZIP της εφαρμογής. Στη συνέχεια το αποσυμπιέζει σε έναν τοπικό φάκελο. Η βασική δομή των φακέλων είναι: 
- project/ back-end/ 
- project/ front-end/ 
- Project/processing/
- 
3. Ρύθμιση της βάσης δεδομένων με XAMPP και HeidiSQL 
Με την προϋπόθεση ότι το XAMPP και το HeidiSQL (ή αντίστοιχη πλατφόρμα) είναι ήδη εγκατεστημένα, ο χρήστης ακολουθεί τα παρακάτω βήματα: 
-Ανοίγει το XAMPP Control Panel 
-Ενεργοποιείται η MySQL πατώντας Start 
-Ανοίγει το HeidiSQL 
-Δημιουργείται ή επιλέγεται σύνδεση τύπου MariaDB or MySQL 
Συμπληρώνονται τα στοιχεία σύνδεσης: 
-Hostname: 127.0.0.1 
- User: root
-Password: ο κωδικός που έχει οριστεί στο XAMPP ή κενό πεδίο 
-Port: 3306 
Μετά τη σύνδεση δημιουργείται νέα βάση δεδομένων με όνομα analyzed_projects. Η βάση μπορεί να δημιουργηθεί από το γραφικό περιβάλλον του HeidiSQL ή με την εντολή: 
CREATE DATABASE analyzed_projects 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci; 
Επιλέγεται η βάση analyzed_projects. Στη συνέχεια, ο χρήστης ανοίγει στο HeidiSQL το αρχείο SQL που περιλαμβάνεται στο ZIP του project. Τέλος, εκτελείται το αρχείο, ώστε να δημιουργηθούν οι απαραίτητοι πίνακες: 
- projects 
- analysis_runs 
- merged_file_metrics 
- project_trend_history 
Μετά την ολοκλήρωση της ρύθμισης, το HeidiSQL μπορεί να κλείσει. Το XAMPP πρέπει να παραμένει ενεργό όσο εκτελείται η εφαρμογή.

4. Ρύθμιση του SonarQube 
Το SonarQube πρέπει να ξεκινήσει πριν από το back-end. Σε περιβάλλον Windows μπορεί να εκτελεστεί το αρχείο StartSonar.bat από τον φάκελο εγκατάστασης του, μέσω της γραμμής εντολών. Μετά την εκκίνησή του, πρέπει να είναι διαθέσιμο στη διεύθυνση http://localhost:9000. Στη συνέχεια ο χρήστης συνδέεται στο SonarQube και μεταβαίνει στις ρυθμίσεις του λογαριασμού του ώστε να δημιουργήσει ένα προσωπικό token με τον εξης τρόπο: 
My Account → Security → Generate Tokens 
Το token αυτό θα καταχωριθεί στο αρχείο .env. Κάθε χρήστης πρέπει να χρησιμοποιεί το δικό του token. Επίσης, ο χρήστης που δημιουργεί το token πρέπει να έχει δικαίωμα εκτέλεσης αναλύσεων και δημιουργίας νέων SonarQube projects.

6. Δημιουργία του αρχείου .env 
Μέσα στον φάκελο back-end δημιουργείται ένα αρχείο με όνομα .env. Το αρχείο περιλαμβάνει τις ρυθμίσεις της MariaDB, του SonarQube και του AllinCode: 
- DB_HOST=127.0.0.1 
- DB_PORT=3306 
- DB_USER=root 
- DB_PASSWORD=   
- DB_NAME=analyzed_projects 
- SONAR_URL=http://localhost:9000 
- SONAR_TOKEN=your_personal_sonarqube_token 
- SONAR_SCANNER_HOME=C:\path\to\sonar-scanner  
- ALLINCODE_BASE_URL=
- ALLINCODE_EMAIL=your_email 
- ALLINCODE_PASSWORD=your_password 
Προαιρετικά μπορούν να οριστούν χρονικά όρια για το AllinCode: 
 
-ALLINCODE_POLL_INTERVAL_MS=10000 
-ALLINCODE_ANALYSIS_TIMEOUT_MS=1800000 
Αν ο χρήστης root διαθέτει κωδικό, συμπληρώνεται στο DB_PASSWORD. Το SONAR_SCANNER_HOME πρέπει να δείχνει στον βασικό φάκελο του SonarScanner και όχι στον υποφάκελο bin. Το .env δεν πρέπει να περιλαμβάνεται σε δημόσιο αποθετήριο ή στο ZIP με πραγματικούς κωδικούς και tokens. Αντί γι’ αυτό, στο ZIP συμπεριλαμβάνεται ένα.env.example με κενές τιμές. 

6. Εγκατάσταση των Python βιβλιοθηκών 
Η βιβλιοθήκη PyDriller είναι απαραίτητη για την επεξεργασία του ιστορικού Git. Η εγκατάστασή της πραγματοποιείται από τη γραμμή εντολών με την εντολή python -m pip install pydriller. Η PyDriller χρησιμοποιείται από το git_metrics.py, ενώ τα analytics_pipeline.py και scoring.py βασίζονται στις βιβλιοθήκες της βασικής εγκατάστασης της Python. 

7. Εγκατάσταση των εξαρτήσεων του back-end και του front-end 
Η εγκατάσταση των εξαρτήσεων πραγματοποιείται μέσω της γραμμής εντολών. Αρχικά, ο χρήστης μεταβαίνει στον φάκελο back-end και εκτελεί την npm install. Αντίστοιχα,μεταβαίνει στον φάκελο front-end και εκτελεί την ίδια εντολη.  
Οι εντολές διαβάζουν το αντίστοιχο αρχείο package.json κάθε φακέλου. Στο back-end εγκαθίστανται 
το Express, το mysql2, το Axios και οι υπόλοιπες απαραίτητες βιβλιοθήκες. Στο front-end 
εγκαθίστανται το React, το Vite, το React Router, το Chart.js και οι υπόλοιπες εξαρτήσεις της διεπαφής 
χρήστη. Η διαδικασία απαιτείται μόνο κατά την πρώτη εγκατάσταση ή όταν αλλάξουν οι εξαρτήσεις 
του project.

9. Εκτέλεση της εφαρμογής 
Πριν ξεκινήσει η εφαρμογή πρέπει να λειτουργούν ήδη το XAMPP και το SonarQube στη θύρα 9000. Στον φακέλο back-end μέσω της γραμμής εντολών εκτελείται η εντολή node server.js. Αν η εκκίνηση ολοκληρωθεί σωστά, εμφανίζεται “Server running on http://localhost:3000”. Το τερματικό αυτό πρέπει να παραμείνει ανοιχτό. 
Αντίστοιχα, στον φακέλο front-end μέσω της γραμμής εντολών εκτελείται η εντολή npm run dev. Το Vite εμφανίζει τη διεύθυνση της εφαρμογής, η οποία είναι συνήθως http://localhost:5173. Το τερματικό του front-end πρέπει επίσης να παραμείνει ανοιχτό. Η εφαρμογή λειτουργεί μέσω φυλλομετρητή στη διεύθυνση που εμφανίζει το Vite. 
